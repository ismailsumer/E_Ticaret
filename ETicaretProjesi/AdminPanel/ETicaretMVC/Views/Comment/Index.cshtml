@model List<AdminPanel.ETicaretMVC.Models.DTOs.CommentViewDto>

<h2 class="mb-4">💬 Yorum Yönetimi</h2>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">@TempData["Success"]</div>
}
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}

<div class="card shadow-sm">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>ID</th>
                        <th>Kullanıcı</th>
                        <th>Ürün</th>
                        <th>Yorum</th>
                        <th>Puan</th>
                        <th>Tarih</th>
                        <th>İşlem</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Any())
                    {
                        @foreach (var c in Model)
                        {
                            <tr>
                                <td>@c.Id</td>
                                <td><strong>@c.Username</strong></td>
                                <td>@c.ProductName</td>
                                <td>@(c.Content.Length > 80 ? c.Content.Substring(0, 80) + "..." : c.Content)</td>
                                <td>
                                    @for (int i = 0; i < c.Rating; i++) { <text>⭐</text> }
                                </td>
                                <td>@c.CreatedDate.ToString("dd.MM.yyyy")</td>
                                <td>
                                    <form asp-action="Delete" method="post" style="display:inline" onsubmit="return confirm('Bu yorumu silmek istediğinize emin misiniz?');">
                                        <input type="hidden" name="id" value="@c.Id" />
                                        <button type="submit" class="btn btn-sm btn-danger">Sil</button>
                                    </form>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr><td colspan="7" class="text-center text-muted">Henüz yorum yok.</td></tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
