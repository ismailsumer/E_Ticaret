@model List<AdminPanel.ETicaretMVC.Controllers.CategoryViewModel>
@{
    ViewData["Title"] = "Kategori Yönetimi";
}

<div class="py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold">📂 Kategori Yönetimi</h2>
    </div>

    <!-- Add Category Form -->
    <div class="card border-0 shadow-sm mb-4">
        <div class="card-body">
            <form asp-action="Create" method="post" class="row g-3 align-items-end">
                <div class="col-md-9">
                    <label class="form-label fw-bold">Yeni Kategori</label>
                    <input type="text" name="name" class="form-control" placeholder="Kategori adı girin..." required />
                </div>
                <div class="col-md-3">
                    <button type="submit" class="btn btn-primary w-100">➕ Ekle</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Category List -->
    <div class="card border-0 shadow-sm">
        <div class="card-body">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Kategori Adı</th>
                        <th class="text-end">İşlem</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var cat in Model)
                    {
                        <tr>
                            <td>@cat.Id</td>
                            <td>@cat.Name</td>
                            <td class="text-end">
                                <form asp-action="Delete" method="post" style="display:inline">
                                    <input type="hidden" name="id" value="@cat.Id" />
                                    <button type="submit" class="btn btn-sm btn-outline-danger" onclick="return confirm('Silmek istediğinize emin misiniz?')">🗑️ Sil</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            @if (!Model.Any())
            {
                <p class="text-center text-muted py-3">Henüz kategori eklenmemiş.</p>
            }
        </div>
    </div>
</div>
