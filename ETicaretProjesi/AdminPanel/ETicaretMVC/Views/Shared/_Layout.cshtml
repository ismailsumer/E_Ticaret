<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>E-Ticaret Admin Panel</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />
    <style>
        body { font-family: 'Inter', sans-serif; background: #f0f2f5; }
        .sidebar {
            background: linear-gradient(180deg, #1a1a2e 0%, #16213e 100%) !important;
            border-right: none !important;
            min-height: 100vh;
        }
        .sidebar .nav-link {
            color: #a8b2d1 !important; padding: 12px 20px; margin: 2px 8px;
            border-radius: 10px; font-size: 0.9rem; font-weight: 500;
            transition: all 0.3s;
        }
        .sidebar .nav-link:hover, .sidebar .nav-link.active {
            color: #fff !important; background: rgba(233, 69, 96, 0.15);
        }
        .sidebar .brand-title {
            color: #e94560; font-size: 1.3rem; font-weight: 700;
            padding: 20px; border-bottom: 1px solid rgba(255,255,255,0.1);
        }
        main { padding-top: 24px; }
    </style>
</head>
<body>
<div class="container-fluid">
    <div class="row">
        <nav id="sidebarMenu" class="col-md-3 col-lg-2 d-md-block sidebar collapse" style="min-height: 100vh;">
            <div class="brand-title">🛒 Admin Panel</div>
            <div class="position-sticky pt-3">
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Home" asp-action="Index">🏠 Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Product" asp-action="Index">📦 Ürünler</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Category" asp-action="Index">📂 Kategoriler</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Brand" asp-action="Index">🏷️ Markalar</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Order" asp-action="Index">🛒 Siparişler</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="User" asp-action="Index">👤 Kullanıcılar</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Comment" asp-action="Index">💬 Yorumlar</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Shipping" asp-action="Index">🚚 Kargo</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Slider" asp-action="Index">🖼️ Slider</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Settings" asp-action="Index">⚙️ Ayarlar</a>
                    </li>
                    <li class="nav-item mt-auto">
                        <a class="nav-link text-danger" asp-controller="Auth" asp-action="Logout">🚪 Çıkış</a>
                    </li>
                </ul>
            </div>
        </nav>

        <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
            @RenderBody()
        </main>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>